package t3generics.game;

// Создали класс Student который является суб-классом Participant
public class Student extends Participant{
    // Переопределили конструктор
    public Student(String name, int age) {
        super(name, age);
    }
}
